<%- include('layout', {body: `
<!-- Modern Hero Section -->
<div class="hero-section">
    <div class="hero-content">
        <div class="container">
            <div class="row align-items-center min-vh-75">
                <div class="col-lg-6">
                    <h1 class="hero-title">
                        Connect. Collaborate. 
                        <span class="gradient-text">Create.</span>
                    </h1>
                    <p class="hero-subtitle">
                        Malaysia's premier platform connecting restaurants with talented food influencers for authentic dining collaborations.
                    </p>
                    <div class="d-flex gap-4 flex-wrap">
                        <a href="/register" class="btn btn-light btn-lg ripple pulse-on-hover magnetic">
                            <i class="fas fa-rocket me-2"></i>Join the Movement
                        </a>
                        <a href="#how-it-works" class="btn btn-outline-light btn-lg ripple bounce-on-hover">
                            <i class="fas fa-play-circle me-2"></i>Learn More
                        </a>
                    </div>
                </div>
                <div class="col-lg-6">
                    <div class="position-relative">
                        <div class="hero-image-placeholder">
                            <div class="floating-cards">
                                <div class="floating-card restaurant-card">
                                    <i class="fas fa-store"></i>
                                    <span>Restaurants</span>
                                </div>
                                <div class="floating-card influencer-card">
                                    <i class="fas fa-camera"></i>
                                    <span>Influencers</span>
                                </div>
                                <div class="floating-card connect-card">
                                    <i class="fas fa-handshake"></i>
                                    <span>Connect</span>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- How It Works Section -->
<section id="how-it-works" class="py-5">
    <div class="container">
        <div class="text-center mb-5">
            <h2 class="gradient-text mb-4">How FoodConnect Malaysia Works</h2>
            <p class="lead text-muted">Simple steps to start your food collaboration journey</p>
        </div>

        <div class="row g-4">
            <div class="col-md-4 reveal">
                <div class="feature-card tilt interactive-element">
                    <div class="feature-icon feature-icon-primary">
                        <i class="fas fa-user-plus"></i>
                    </div>
                    <h4 class="mb-3 gradient-text">Sign Up</h4>
                    <p class="text-muted">Create your account as a restaurant owner or food influencer. Complete your profile to get started.</p>
                </div>
            </div>
            <div class="col-md-4 reveal">
                <div class="feature-card tilt interactive-element">
                    <div class="feature-icon feature-icon-emerald">
                        <i class="fas fa-search"></i>
                    </div>
                    <h4 class="mb-3 gradient-text-emerald">Discover & Connect</h4>
                    <p class="text-muted">Restaurants post campaigns, influencers browse and apply. Find the perfect match for your collaboration needs.</p>
                </div>
            </div>
            <div class="col-md-4 reveal">
                <div class="feature-card tilt interactive-element">
                    <div class="feature-icon feature-icon-gold">
                        <i class="fas fa-handshake"></i>
                    </div>
                    <h4 class="mb-3 gradient-text-gold">Collaborate</h4>
                    <p class="text-muted">Work together to create amazing content. Track progress and manage payments through our platform.</p>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- Features Section -->
<section class="py-5 bg-white">
    <div class="container">
        <div class="row g-5">
            <div class="col-lg-6 reveal-left">
                <div class="card dashboard-card h-100 tilt interactive-element">
                    <div class="card-body">
                        <div class="d-flex align-items-center mb-4">
                            <div class="feature-icon feature-icon-primary me-4 bounce-on-hover" style="width: 60px; height: 60px; font-size: 1.5rem;">
                                <i class="fas fa-store"></i>
                            </div>
                            <h3 class="gradient-text mb-0">For Restaurants</h3>
                        </div>
                        <ul class="list-unstyled mb-4">
                            <li class="mb-3 d-flex align-items-center reveal">
                                <i class="fas fa-check-circle me-3" style="color: var(--emerald);"></i>
                                <span>Post collaboration campaigns</span>
                            </li>
                            <li class="mb-3 d-flex align-items-center reveal">
                                <i class="fas fa-check-circle me-3" style="color: var(--emerald);"></i>
                                <span>Control advertising costs</span>
                            </li>
                            <li class="mb-3 d-flex align-items-center reveal">
                                <i class="fas fa-check-circle me-3" style="color: var(--emerald);"></i>
                                <span>Manage applications and collaborations</span>
                            </li>
                            <li class="mb-3 d-flex align-items-center reveal">
                                <i class="fas fa-check-circle me-3" style="color: var(--emerald);"></i>
                                <span>Build authentic partnerships</span>
                            </li>
                        </ul>
                        <a href="/register?type=restaurant" class="btn btn-primary btn-lg w-100 ripple glow-on-hover">
                            <i class="fas fa-store me-2"></i>Get Started as Restaurant
                        </a>
                    </div>
                </div>
            </div>
            <div class="col-lg-6 reveal-right">
                <div class="card dashboard-card h-100 tilt interactive-element">
                    <div class="card-body">
                        <div class="d-flex align-items-center mb-4">
                            <div class="feature-icon feature-icon-emerald me-4 bounce-on-hover" style="width: 60px; height: 60px; font-size: 1.5rem;">
                                <i class="fas fa-camera"></i>
                            </div>
                            <h3 class="gradient-text-emerald mb-0">For Influencers</h3>
                        </div>
                        <ul class="list-unstyled mb-4">
                            <li class="mb-3 d-flex align-items-center reveal">
                                <i class="fas fa-check-circle me-3" style="color: var(--orange);"></i>
                                <span>Discover exciting restaurant partnerships</span>
                            </li>
                            <li class="mb-3 d-flex align-items-center reveal">
                                <i class="fas fa-check-circle me-3" style="color: var(--orange);"></i>
                                <span>Showcase your content portfolio</span>
                            </li>
                            <li class="mb-3 d-flex align-items-center reveal">
                                <i class="fas fa-check-circle me-3" style="color: var(--orange);"></i>
                                <span>Apply to campaigns that match you</span>
                            </li>
                            <li class="mb-3 d-flex align-items-center reveal">
                                <i class="fas fa-check-circle me-3" style="color: var(--orange);"></i>
                                <span>Secure fair compensation</span>
                            </li>
                        </ul>
                        <a href="/register?type=influencer" class="btn btn-lg w-100 ripple glow-on-hover" style="background: var(--emerald-gradient); color: white;">
                            <i class="fas fa-camera me-2"></i>Join as Influencer
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- MVP Launch Notice -->
<section class="py-5">
    <div class="container">
        <div class="mvp-notice">
            <div class="row align-items-center">
                <div class="col-md-8">
                    <h3 class="mb-2">🚀 MVP Launching October 2025</h3>
                    <p class="mb-0 opacity-90">Join the waiting list now and be among the first to experience Malaysia's premier food collaboration platform!</p>
                </div>
                <div class="col-md-4 text-md-end mt-3 mt-md-0">
                    <a href="/register" class="btn btn-light btn-lg ripple pulse-on-hover magnetic">
                        <i class="fas fa-star me-2"></i>Join Waiting List
                    </a>
                </div>
            </div>
        </div>
    </div>
</section>

<style>
/* Hero Section Enhancements */
.min-vh-75 {
    min-height: 75vh;
}

.btn-outline-light {
    border: 2px solid rgba(255, 255, 255, 0.8);
    color: white;
    background: transparent;
}

.btn-outline-light:hover {
    background: white;
    color: var(--primary);
    border-color: white;
    transform: translateY(-2px);
    box-shadow: var(--shadow-lg);
}

/* Floating Cards Animation */
.hero-image-placeholder {
    position: relative;
    height: 400px;
    display: flex;
    align-items: center;
    justify-content: center;
}

.floating-cards {
    position: relative;
    width: 300px;
    height: 300px;
}

.floating-card {
    position: absolute;
    background: rgba(255, 255, 255, 0.1);
    backdrop-filter: blur(25px) saturate(200%);
    border: 1px solid rgba(255, 255, 255, 0.2);
    padding: 1.5rem;
    border-radius: 1rem;
    box-shadow: 
        0 8px 32px rgba(0, 0, 0, 0.1),
        inset 0 1px 0 rgba(255, 255, 255, 0.3);
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
    font-weight: 600;
    color: white;
    animation: float-gentle 6s ease-in-out infinite;
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    text-shadow: 0 2px 8px rgba(0, 0, 0, 0.3);
    position: relative;
    overflow: hidden;
}

.floating-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: linear-gradient(45deg, transparent 30%, rgba(255, 255, 255, 0.1) 50%, transparent 70%);
    transform: translateX(-100%) translateY(-100%);
    transition: transform 0.6s;
}

.floating-card:hover {
    transform: translateY(-10px) scale(1.08);
    box-shadow: 
        0 20px 60px rgba(0, 0, 0, 0.2),
        inset 0 1px 0 rgba(255, 255, 255, 0.5);
    background: rgba(255, 255, 255, 0.15);
}

.floating-card:hover::before {
    transform: translateX(100%) translateY(100%);
}

.floating-card i {
    font-size: 2rem;
    margin-bottom: 0.5rem;
}

.restaurant-card {
    top: 20px;
    left: 20px;
    color: var(--primary);
}

.influencer-card {
    top: 20px;
    right: 20px;
    color: var(--success);
    animation-delay: -2s;
}

.connect-card {
    bottom: 20px;
    left: 50%;
    transform: translateX(-50%);
    color: var(--warning);
    animation-delay: -4s;
}

@keyframes float-gentle {
    0%, 100% {
        transform: translateY(0px) translateX(0px);
    }
    33% {
        transform: translateY(-10px) translateX(5px);
    }
    66% {
        transform: translateY(5px) translateX(-5px);
    }
}

/* Responsive adjustments */
@media (max-width: 768px) {
    .floating-cards {
        width: 250px;
        height: 250px;
    }
    
    .floating-card {
        padding: 1rem;
        font-size: 0.9rem;
    }
    
    .floating-card i {
        font-size: 1.5rem;
    }
}
</style>

<script>
document.addEventListener('DOMContentLoaded', function() {
    // Check for existing authentication
    const token = localStorage.getItem('token');
    if (token) {
        // User is logged in, update CTA buttons
        const ctaButtons = document.querySelectorAll('a[href="/register"]');
        ctaButtons.forEach(btn => {
            btn.href = '/dashboard';
            btn.innerHTML = '<i class="fas fa-tachometer-alt me-2"></i>Go to Dashboard';
        });
    }
    
    // Smooth scrolling for anchor links
    document.querySelectorAll('a[href^="#"]').forEach(anchor => {
        anchor.addEventListener('click', function (e) {
            e.preventDefault();
            const target = document.querySelector(this.getAttribute('href'));
            if (target) {
                target.scrollIntoView({
                    behavior: 'smooth',
                    block: 'start'
                });
            }
        });
    });

    // Scroll reveal animations
    const observerOptions = {
        threshold: 0.1,
        rootMargin: '0px 0px -50px 0px'
    };

    const observer = new IntersectionObserver(function(entries) {
        entries.forEach(function(entry) {
            if (entry.isIntersecting) {
                entry.target.classList.add('active');
                // Stagger animation for list items
                if (entry.target.classList.contains('reveal')) {
                    const siblings = entry.target.parentElement.querySelectorAll('.reveal');
                    siblings.forEach(function(sibling, index) {
                        setTimeout(function() {
                            sibling.classList.add('active');
                        }, index * 150);
                    });
                }
            }
        });
    }, observerOptions);

    // Observe all elements with reveal classes
    document.querySelectorAll('.reveal, .reveal-left, .reveal-right').forEach(function(el) {
        observer.observe(el);
    });

    // Add floating animation to hero cards
    const floatingCards = document.querySelectorAll('.floating-card');
    floatingCards.forEach(function(card, index) {
        card.style.animationDelay = (index * 0.5) + 's';
    });

    // Add parallax effect to hero section (subtle)
    let ticking = false;
    
    function updateParallax() {
        const scrolled = window.pageYOffset;
        const parallax = document.querySelector('.hero-section');
        if (parallax) {
            const speed = scrolled * 0.3;
            parallax.style.transform = 'translateY(' + speed + 'px)';
        }
        ticking = false;
    }

    function requestTick() {
        if (!ticking) {
            requestAnimationFrame(updateParallax);
            ticking = true;
        }
    }

    window.addEventListener('scroll', requestTick);

    // Add dynamic gradient shift based on scroll
    let scrollPosition = 0;
    window.addEventListener('scroll', function() {
        scrollPosition = window.scrollY / window.innerHeight;
        document.body.style.setProperty('--scroll-position', scrollPosition);
    });

    // Enhanced button interactions
    document.querySelectorAll('.btn').forEach(function(btn) {
        btn.addEventListener('mouseenter', function(e) {
            if (this.classList.contains('magnetic')) {
                this.style.transform = 'scale(1.05) translateY(-2px)';
            }
        });
        
        btn.addEventListener('mouseleave', function(e) {
            if (this.classList.contains('magnetic')) {
                this.style.transform = 'scale(1) translateY(0)';
            }
        });
    });

    // Add sparkle effect on hero title
    function createSparkle(element) {
        const sparkle = document.createElement('span');
        sparkle.classList.add('sparkle');
        sparkle.style.position = 'absolute';
        sparkle.style.left = Math.random() * 100 + '%';
        sparkle.style.top = Math.random() * 100 + '%';
        sparkle.style.fontSize = '0.5rem';
        sparkle.style.color = '#fbbf24';
        sparkle.innerHTML = '✨';
        sparkle.style.animation = 'sparkle 2s ease-in-out infinite';
        sparkle.style.pointerEvents = 'none';
        
        element.appendChild(sparkle);
        
        setTimeout(function() {
            sparkle.remove();
        }, 2000);
    }

    const heroTitle = document.querySelector('.hero-title');
    if (heroTitle) {
        heroTitle.style.position = 'relative';
        setInterval(function() {
            if (Math.random() < 0.3) {
                createSparkle(heroTitle);
            }
        }, 1000);
    }
});
</script>
`}) %>